[project]
name = "pdf-anki-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.120.4",
]

[tool.uv.scripts]
dev = "uvicorn main:app --reload"
